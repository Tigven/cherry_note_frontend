var config = {
    // API settings
	'api_schema': 'http',
	'api_host': '199.217.118.251',
	'api_port': 8000,
	'api_path': '/api/',

    // number of milliseconds of delay before sending modified
    // note's content to the server
    'content_change_delay': 750
}

export default config;